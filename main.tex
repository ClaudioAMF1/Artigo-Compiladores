% !TEX program = pdflatex
% Artigo: Uso de Modelos de Linguagem para Otimizacao de Codigo Python
% Formato: IEEE Conference Template (IEEEtran)
% Compilacao no Overleaf: selecionar compilador pdfLaTeX

\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[brazil]{babel}
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{url}
\usepackage{listings}
\usepackage{hyperref}

\lstset{
  basicstyle=\ttfamily\footnotesize,
  breaklines=true,
  frame=single,
  numbers=left,
  numberstyle=\tiny,
  language=Python,
  showstringspaces=false,
  tabsize=2
}

\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}

\begin{document}

\title{Uso de Modelos de Linguagem para Otimização de Código Python: Limites e Possibilidades em Linguagens Dinâmicas}

\author{
\IEEEauthorblockN{Nome do(a) Autor(a)}
\IEEEauthorblockA{\textit{Curso de Ciência da Computação} \\
\textit{Universidade / Instituto}\\
Cidade, Brasil \\
email@dominio.edu.br}
}

\maketitle

\begin{abstract}
Modelos de Linguagem de Grande Escala (\textit{Large Language Models} --- LLMs) têm apresentado desempenho expressivo em tarefas de engenharia de software, incluindo transformação e otimização de código. Em linguagens dinâmicas como Python, otimizações clássicas de compilador são historicamente limitadas pelo dinamismo da tipagem, \textit{late binding} e reflexão, fatores que reduzem o espaço de decisão de um compilador estático. Este artigo investiga os limites e possibilidades do uso de LLMs como componente auxiliar ao processo de compilação de código Python, propondo uma arquitetura híbrida (análise estática via AST + LLM + validação por testes) avaliada sobre o dataset público HumanEval (OpenAI), composto por 164 problemas Python com testes unitários. Conduzimos uma comparação pareada de \textbf{quatro back-ends} de otimização: um baseline determinístico de regras sobre AST e \textbf{três LLMs de provedores e famílias distintas} (Gemini 2.5 Flash Lite, Llama 3.3 70B via Groq e GPT-4o-mini via GitHub Models). Os resultados quantificam um \textit{trade-off} estruturado: o baseline preserva semântica em 100\% dos casos mas seu \textit{speedup} mediano é estatisticamente indistinguível de $1{,}0\times$ (IC 95\% \textit{bootstrap} $[0{,}997;~1{,}008]$). Entre os LLMs, observa-se forte heterogeneidade: o Gemini melhora 54{,}3\% dos problemas com \textit{speedup} mediano significativo ($1{,}138\times$, IC $[1{,}071;~1{,}220]$) e taxa de alucinação de 6{,}1\%; o GPT-4o-mini melhora 37{,}2\% com mediana $1{,}018\times$ (não significativo) e alucinação de 17{,}7\%; o Llama 70B melhora 27{,}4\% com mediana $0{,}976\times$ (não significativo, inclusive abaixo de 1) e alucinação de 23{,}2\%. Esse resultado refuta a tese simplista ``\textit{LLMs superam baseline}'' e estabelece uma tese mais precisa: \emph{a escolha do modelo importa tanto quanto a decisão de usar um LLM} --- somente o LLM melhor calibrado para a tarefa de transformação de código produz ganho mediano significativo, enquanto LLMs de outras famílias apresentam taxas de alucinação que neutralizam suas melhorias. Os achados justificam arquiteturas híbridas com validação semântica obrigatória e seleção empírica de modelo.
\end{abstract}

\begin{IEEEkeywords}
Compiladores, Otimização de Código, Python, Linguagens Dinâmicas, Modelos de Linguagem, LLM, Gemini, Llama, GPT-4o, HumanEval, AST, Avaliação Empírica Multi-Modelo.
\end{IEEEkeywords}

\section{Introdução}

A crescente adoção de Python em domínios como ciência de dados, aprendizado de máquina e automação reforçou a importância de seu desempenho em tempo de execução. Apesar disso, o interpretador de referência CPython oferece otimizações relativamente modestas se comparado a cadeias de compilação maduras como LLVM ou GCC \cite{cpython_dumb}. Essa lacuna decorre, em grande parte, de características da própria linguagem: tipagem dinâmica, \textit{late binding}, \textit{monkey patching} e capacidade de introspecção e modificação do programa em tempo de execução \cite{yashwanth_dynamic}.

Simultaneamente, Modelos de Linguagem de Grande Escala (LLMs) baseados em \textit{Transformers} vêm sendo empregados com sucesso em tarefas de transformação e otimização de código \cite{cummins2023llm,cummins2024metacompiler,survey2025}. Recentemente, trabalhos específicos têm demonstrado que LLMs podem sugerir transformações de código que resultam em ganhos expressivos de tempo de execução em Python \cite{shypula2023pie,fasterpy2025}. A hipótese central deste trabalho é que LLMs podem atuar como um componente híbrido, complementar às fases de otimização de um compilador Python, operando no nível do código-fonte e no nível do \textit{bytecode}, para reduzir os gargalos impostos pelo dinamismo da linguagem. Para evitar conclusões baseadas em um único modelo, executamos o pipeline proposto em \textbf{quatro configurações pareadas} sobre os 164 problemas do HumanEval: um baseline determinístico de regras AST e três LLMs de provedores e famílias distintas --- \textbf{Gemini 2.5 Flash Lite} (Google), \textbf{Llama 3.3 70B} (Meta, via Groq) e \textbf{GPT-4o-mini} (OpenAI, via GitHub Models).

Este artigo se propõe a responder à seguinte pergunta de pesquisa: \textit{quais são os limites e as possibilidades do uso de LLMs para otimizar código Python no contexto de uma cadeia de compilação, considerando as restrições impostas por uma linguagem dinâmica?}

As contribuições deste trabalho são: (i) um levantamento crítico do estado da arte sobre LLMs aplicados a otimização de código, com foco em Python; (ii) uma análise sistemática dos limites impostos pelo dinamismo da linguagem às otimizações estáticas; (iii) uma proposta e implementação de arquitetura híbrida (análise estática via AST + LLM intercambiável + validação por testes) operando sobre o dataset público HumanEval e suportando múltiplos provedores (Google, Groq, OpenAI/GitHub Models, OpenAI direto, Together); e (iv) um estudo empírico pareado e \textbf{multi-modelo} sobre os 164 problemas do HumanEval, comparando três LLMs de famílias distintas contra um otimizador determinístico de regras AST e quantificando, com testes de significância (\textit{t}-pareado, IC 95\% \textit{bootstrap}), preservação semântica, taxa de alucinação, ganhos e regressões de desempenho.

O restante do artigo está organizado da seguinte forma. A Seção~\ref{sec:fundamentacao} apresenta a fundamentação teórica sobre compilação de linguagens dinâmicas e LLMs para código. A Seção~\ref{sec:relacionados} discute trabalhos correlatos. A Seção~\ref{sec:metodologia} descreve a arquitetura proposta, o \textit{foundation model} e o \textit{dataset} escolhidos. A Seção~\ref{sec:resultados} apresenta os resultados experimentais do baseline determinístico e do back-end Gemini, com testes de significância estatística. A Seção~\ref{sec:discussao} discute limites e possibilidades. A Seção~\ref{sec:ameacas} elenca ameaças à validade. A Seção~\ref{sec:conclusao} apresenta as conclusões e indica trabalhos futuros.

\section{Fundamentação Teórica}
\label{sec:fundamentacao}

\subsection{Linguagens dinâmicas}
Linguagens dinâmicas caracterizam-se pela ausência de verificação de tipos em tempo de compilação e pela resolução tardia (\textit{late binding}) de nomes, métodos e atributos. Esse modelo privilegia a flexibilidade --- o programador pode alterar a estrutura de um programa em tempo de execução, redefinir métodos de classes (\textit{monkey patching}) e introspectar quaisquer objetos --- ao custo da previsibilidade necessária para análises estáticas globais. Em termos de compilação, essas características reduzem drasticamente o conjunto de otimizações aplicáveis sem hipóteses adicionais sobre o comportamento do programa.

\subsection{Python e desempenho}
O CPython, implementação de referência da linguagem, é uma máquina virtual de pilha que interpreta \textit{bytecode} produzido por um pipeline clássico: análise léxica, sintática, construção da AST, geração de um grafo de fluxo de controle e emissão de \textit{bytecode}. Historicamente, o compilador realiza poucas otimizações, limitando-se a \textit{constant folding} e dobramento de expressões simples \cite{cpython_dumb}. A partir do CPython 3.11, o \textit{specializing adaptive interpreter} (PEP 659) introduziu especialização dinâmica de instruções do \textit{bytecode} em função dos tipos observados em tempo de execução.

Os principais gargalos de desempenho em Python são bem caracterizados \cite{yashwanth_dynamic}: (i) laços interpretados, em razão do \textit{overhead} por iteração da máquina virtual; (ii) alocação intensa de objetos, devido à natureza dos tipos primitivos da linguagem como objetos completos; e (iii) \textit{dynamic dispatch}, decorrente da resolução de métodos em tempo de execução. Abordagens alternativas como PyPy, Numba, Cython e mypyc \cite{cython_docs} buscam contornar o dinamismo assumindo tipos (via anotações ou \textit{profiling}) para gerar código nativo mais eficiente, sempre dependendo, contudo, de restrições adicionais impostas ao programador.

\subsection{Modelos de Linguagem (LLMs)}
\textit{Foundation models} baseados em arquitetura \textit{Transformer} são treinados em grandes corpora de linguagem natural e código-fonte, internalizando padrões sintáticos, semânticos e algorítmicos. Modelos como GPT-4o, Claude 3.5, Code Llama, DeepSeek-Coder e StarCoder2 demonstram capacidade não apenas de \textbf{gerar código} a partir de descrições em linguagem natural, mas também de \textbf{refatorar} e \textbf{otimizar} código existente \cite{cummins2024metacompiler,survey2025}. Diferentemente de um compilador clássico, um LLM opera de maneira probabilística, não garantindo equivalência semântica a priori; em compensação, é capaz de aplicar transformações de alto nível que dependem de conhecimento algorítmico amplo, fora do alcance de heurísticas estáticas. Essa propriedade torna LLMs especialmente promissores para linguagens dinâmicas, justamente onde o compilador clássico encontra suas maiores barreiras.

\subsection{Otimização de código}
Otimizações de código podem ser classificadas em três níveis: (i) \textbf{micro-otimizações}, que atuam sobre instruções individuais ou pequenos trechos (e.g., substituição de operadores, \textit{constant folding}); (ii) \textbf{refatoração estrutural}, que reorganiza algoritmos ou estruturas de dados (e.g., memoização, vetorização, troca de estrutura de dados); e (iii) \textbf{paralelização}, que explora execução concorrente em múltiplos núcleos. As métricas usuais de avaliação são \textbf{tempo de execução}, \textbf{uso de memória} e, secundariamente, \textbf{legibilidade} e custo de manutenção do código transformado. Em Python, dado o GIL e o \textit{overhead} da máquina virtual, os ganhos mais substanciais costumam vir do segundo nível, onde LLMs são particularmente competentes.

\subsection{Limitações impostas pelo dinamismo}
As características das linguagens dinâmicas restringem otimizações estáticas clássicas:
\begin{itemize}
  \item \textbf{Tipagem dinâmica:} o tipo de uma variável pode variar ao longo da execução, dificultando especialização e \textit{inline caching}.
  \item \textbf{Late binding:} a resolução de métodos e funções ocorre em tempo de execução, o que inviabiliza \textit{inlining} estático agressivo.
  \item \textbf{Reflexão e metaprogramação:} construções como \texttt{getattr}, \texttt{setattr}, \texttt{eval} e \texttt{exec} permitem alterar o programa em execução, comprometendo análises globais.
  \item \textbf{Monkey patching:} a possibilidade de substituir atributos de classes em tempo de execução anula garantias sobre tabelas de métodos.
  \item \textbf{GIL:} o \textit{Global Interpreter Lock} restringe o paralelismo real, impondo escolhas específicas ao compilador.
\end{itemize}

Esses fatores configuram o que denominamos aqui de \textbf{fronteira de otimização estática}: um conjunto de transformações que compiladores convencionais não podem aplicar sem hipóteses adicionais sobre o comportamento do programa.

\section{Trabalhos Relacionados}
\label{sec:relacionados}

Cummins et al. \cite{cummins2023llm} demonstraram que um LLM treinado em um corpus de código LLVM-IR pode prever sequências de passes de otimização capazes de reduzir significativamente o tamanho do código gerado. O trabalho foi posteriormente estendido no \textit{Meta Large Language Model Compiler}, uma família de modelos treinada em 546 bilhões de \textit{tokens} de IR e assembly, voltada explicitamente a tarefas de otimização \cite{cummins2024metacompiler}.

Shypula et al. \cite{shypula2023pie} introduziram o dataset \textit{Performance Improving Edits} (PIE), composto por pares de programas (versão lenta / versão rápida) derivados do IBM Project CodeNet, com extensos testes unitários. O trabalho mostrou que modelos como GPT-3.5 ajustados com estratégias de \textit{retrieval}, \textit{performance-conditioning} e \textit{self-play} podem alcançar \textit{speedups} médios de até 6{,}86$\times$ em tarefas de otimização de código competitivo.

Mais recentemente, \textit{FasterPy} \cite{fasterpy2025} propôs um \textit{framework} específico para otimização de execução de código Python baseado em LLMs, combinando RAG com adaptação parâmetro-eficiente (LoRA) sobre o \textit{benchmark} PIE. Já o \textit{CompilerGPT} \cite{compilergpt2025} automatiza a interação entre compiladores tradicionais (Clang, GCC) e LLMs a partir da leitura de relatórios de otimização, obtendo \textit{speedups} de até 6{,}5$\times$. O \textit{SysLLMatic}, por sua vez, reporta melhorias médias de 1{,}85$\times$ em latência e 2{,}24$\times$ em \textit{throughput} em aplicações de larga escala \cite{sysllmatic2025}.

Em uma direção metodológica complementar, o \textit{DocAgent} \cite{yang2025docagent} introduz um sistema multi-agente para geração automatizada de documentação de código que combina análise estática via AST, construção de um grafo de dependências entre componentes (funções, classes, métodos), travessia topológica para construção incremental de contexto e agentes especializados (\textit{Reader}, \textit{Searcher}, \textit{Writer}, \textit{Verifier}, \textit{Orchestrator}). Embora o problema-alvo seja a geração de \textit{docstrings}, e não otimização, dois aspectos da arquitetura do DocAgent são diretamente transponíveis para o pipeline proposto neste artigo: (a) o uso da AST como representação canônica para extrair entidades e suas dependências, evitando que o LLM opere sobre o código-fonte como texto cego; e (b) a presença de um agente \textit{Verifier} que valida factualmente a saída do LLM contra o repositório, mitigando alucinações. Esse padrão é análogo ao módulo de validação por testes unitários adotado em nossa metodologia (Seção~\ref{sec:metodologia}).

Em conjunto, esses trabalhos indicam que LLMs são ferramentas competitivas, e muitas vezes complementares, aos compiladores clássicos, principalmente quando as otimizações ocorrem em um nível semântico ou algorítmico mais alto, e que arquiteturas que acoplam análise estática (AST, grafos de dependência) a um \textit{loop} de geração-com-verificação tendem a produzir resultados mais confiáveis do que prompts diretos sobre o código-fonte.

\section{Metodologia e Arquitetura Proposta}
\label{sec:metodologia}

\subsection{Foundation Models}
Para evitar conclusões enviesadas por um único modelo, este trabalho avalia \textbf{três LLMs de provedores e famílias distintas}, todos acessados via tier gratuito sem custo monetário:

\begin{itemize}
  \item \textbf{Gemini 2.5 Flash Lite} (Google): acessado via REST do Google AI Studio. Modelo proprietário, arquitetura compacta otimizada para latência baixa.
  \item \textbf{Llama 3.3 70B} (Meta): acessado via API Groq, que serve o modelo open-weight em hardware especializado de inferência. Modelo aberto, 70 bilhões de parâmetros.
  \item \textbf{GPT-4o-mini} (OpenAI): acessado via GitHub Models (Microsoft), que oferece tier gratuito para o ecossistema OpenAI mediante PAT de escopo \texttt{Models:Read}. Modelo proprietário, classe média.
\end{itemize}

Todos os três foram configurados com os mesmos hiperparâmetros (\textit{temperature} = $0{,}2$, \textit{max\_output\_tokens} = $2048$) e receberam o mesmo \textit{prompt} estruturado em português, garantindo comparabilidade. Cada provedor expõe API compatível com o SDK da OpenAI ou um endpoint REST simples, permitindo intercambialidade transparente no pipeline. Para erros transitórios (HTTP 429/5xx, comuns em \textit{tier} gratuito), o cliente implementa \textit{retry} exponencial de até 7 tentativas com \textit{backoff} de até 60\,s.

Para isolar o efeito do paradigma LLM em si, é adicionalmente implementado um otimizador determinístico baseado em transformações sobre AST, utilizado como \textbf{linha de base (\textit{baseline})}. A comparação pareada entre o baseline e os três LLMs permite separar três efeitos: (a) o ganho do paradigma simbólico vs. probabilístico, (b) a variação entre famílias de LLMs distintas, e (c) o efeito da escolha específica de modelo dentro do paradigma LLM.

\subsection{Dataset}
Como objeto de pesquisa, é utilizado o \textbf{HumanEval} \cite{chen2021humaneval}, \textit{dataset} público mantido pela OpenAI e disponível em repositório Git aberto\footnote{\url{https://github.com/openai/human-eval}}. O HumanEval é composto por 164 problemas de programação em Python, cada um contendo: (i) um cabeçalho com assinatura, \textit{docstring} e exemplos; (ii) uma solução canônica (\texttt{canonical\_solution}); e (iii) uma função \texttt{check(candidate)} contendo as asserções que definem a corretude funcional. A escolha do HumanEval deve-se à sua ampla adoção como referência em pesquisa de Code LLMs, à qualidade de seus testes unitários e ao fato de ser 100\% Python. Para o papel de repositório auxiliar de exemplos de otimização (\textit{few-shot}), o dataset PIE \cite{shypula2023pie} permanece como fonte de pares (versão lenta, versão rápida) para futuras extensões.

\subsection{Arquitetura do Pipeline}
A arquitetura proposta está organizada em cinco módulos, descritos a seguir:

\begin{enumerate}
    \item \textbf{Módulo de Ingestão:} lê os 164 problemas do HumanEval diretamente do arquivo \texttt{HumanEval.jsonl.gz} fornecido pelo repositório oficial, extraindo \texttt{prompt}, \texttt{canonical\_solution}, \texttt{entry\_point} e \texttt{test}.
    \item \textbf{Módulo de Análise Estática:} constrói a AST do código completo (\texttt{prompt + canonical\_solution}) via módulo \texttt{ast} da biblioteca padrão e extrai \textit{features} numéricas: quantidade de funções, laços, profundidade máxima de aninhamento, chamadas de função, chamadas recursivas ao \textit{entry point}, \textit{comprehensions} e presença de anotações de tipo.
    \item \textbf{Módulo de Otimização:} aceita dois \textit{back-ends} intercambiáveis via parâmetro de configuração: (a) \texttt{gemini} --- encaminha o código e as \textit{features} ao Gemini 2.5 Flash Lite por meio de um \textit{prompt} estruturado em português que exige preservação rigorosa da assinatura e da semântica; (b) \texttt{rule\_based} --- aplica uma sequência determinística de transformações sobre a AST: R1 memoização via \texttt{functools.lru\_cache}, R2 substituição de somatório manual por \texttt{sum()}, R4 \texttt{frozenset} para testes de pertencimento e R5 \texttt{join} para concatenação de strings em laço.
    \item \textbf{Módulo de Validação:} compila o código retornado, executa a função \texttt{check(candidate)} do HumanEval sob \texttt{SIGALRM} com \textit{timeout} de 10\,s e descarta a transformação caso qualquer asserção falhe ou haja exceção.
    \item \textbf{Módulo de \textit{Benchmark}:} constrói um \textit{workload} reprodutível a partir das próprias chamadas \texttt{candidate(\ldots)} presentes no teste oficial do problema, executa o \textit{workload} \textbf{200 vezes em cada uma de 5 repetições} e reporta o tempo mínimo (para minimizar ruído). O \textit{speedup} é definido por $s = t_\text{orig}^\text{min}/t_\text{opt}^\text{min}$.
\end{enumerate}

O pipeline completo pode ser interpretado como um \textit{loop} assistido por \textit{feedback}: falhas detectadas no módulo de validação podem ser reintroduzidas no \textit{prompt} em uma estratégia análoga ao \textit{self-refine}.

\subsection{Métricas de Avaliação}
A avaliação considera quatro métricas: (i) \textbf{taxa de preservação semântica}, proporção de códigos otimizados que passam em todos os testes do HumanEval; (ii) \textbf{speedup} médio aritmético, mediano e harmônico relativo à versão original; (iii) \textbf{taxa de melhoria}, fração de problemas com speedup superior a $1{,}05\times$; e (iv) \textbf{taxa de regressão}, fração de problemas com speedup inferior a $0{,}95\times$. Adicionalmente é reportada a distribuição de regras disparadas no \textit{baseline} determinístico.

\subsection{Exemplo Ilustrativo}
A Listagem~\ref{lst:original} apresenta um código tipicamente encontrado em \textit{datasets} de programação competitiva, e a Listagem~\ref{lst:otimizado} mostra uma transformação esperada do LLM.

\begin{lstlisting}[caption={Trecho original com laço explícito.}, label={lst:original}]
def soma_quadrados(n):
    total = 0
    for i in range(n):
        total = total + i * i
    return total
\end{lstlisting}

\begin{lstlisting}[caption={Versão otimizada sugerida pelo LLM.}, label={lst:otimizado}]
def soma_quadrados(n):
    # formula fechada: n*(n-1)*(2n-1)/6
    return n * (n - 1) * (2 * n - 1) // 6
\end{lstlisting}

Este tipo de transformação algorítmica dificilmente seria produzido por um compilador clássico, pois exige conhecimento semântico sobre o domínio matemático envolvido.

\section{Resultados Experimentais}
\label{sec:resultados}

O pipeline descrito na Seção~\ref{sec:metodologia} foi executado sobre \textbf{todos os 164 problemas} do HumanEval utilizando o \textit{back-end} determinístico baseado em regras AST (\texttt{rule\_based}) como linha de base. Os experimentos foram conduzidos em CPython~3.11 sobre Linux, com cada \textit{workload} de \textit{benchmark} executado 200 vezes em cada uma de 5 repetições, reportando-se o tempo mínimo.

\subsection{Métricas Agregadas}

\begin{table}[htbp]
\caption{Resultados agregados sobre os 164 problemas do HumanEval (baseline determinístico via AST).}
\label{tab:agregado}
\centering
\begin{tabular}{lr}
\hline
\textbf{Métrica} & \textbf{Valor} \\
\hline
Problemas avaliados & 164 \\
Preservação semântica & 164/164 (100{,}0\%) \\
Problemas melhorados ($>$1{,}05$\times$) & 15 \\
Problemas neutros [0{,}95;~1{,}05] & 126 \\
Regressões ($<$0{,}95$\times$) & 14 \\
Imensuráveis (clock $<$ 1$\mu$s) & 9 \\
Regras disparadas (pelo menos uma) & 8 \\
\hline
Speedup médio aritmético\textsuperscript{$\dagger$} & 5{,}927$\times$ \\
Speedup mediano\textsuperscript{$\dagger$} & 1{,}003$\times$ \\
Speedup médio harmônico\textsuperscript{$\dagger$} & 0{,}986$\times$ \\
Speedup máximo\textsuperscript{$\dagger$} & 615{,}378$\times$ \\
Speedup mínimo\textsuperscript{$\dagger$} & 0{,}361$\times$ \\
\hline
\end{tabular}
\vspace{0.1cm}
\raggedright
\scriptsize \textsuperscript{$\dagger$}{Estatísticas calculadas sobre os 155 problemas com tempo mensurável; os 9 problemas em que tanto o tempo original quanto o otimizado caíram abaixo da resolução do clock (1$\mu$s) são excluídos por não fornecerem razão definida.}
\end{table}

A Tabela~\ref{tab:agregado} sintetiza os resultados. A \textbf{preservação semântica é total}: nenhuma das 164 transformações geradas pelo otimizador determinístico quebrou os testes do HumanEval. Contudo, a grande maioria dos problemas (126 de 164) apresenta \textit{speedup} neutro (entre 0{,}95$\times$ e 1{,}05$\times$), indicando que nenhum padrão sintático das regras implementadas foi detectado. O \textit{speedup} médio aritmético (5{,}927$\times$) é inflado por dois \textit{outliers} de memoização, enquanto o \textit{speedup} mediano (1{,}003$\times$) e a média harmônica (0{,}986$\times$) refletem mais fielmente o impacto típico.

\subsubsection*{Significância estatística}
A diferença pareada $\Delta_i = t_i^\text{orig} - t_i^\text{opt}$ entre tempos original e otimizado, calculada sobre os 164 problemas, possui média $\bar{\Delta} = 0{,}173$~ms com erro padrão $\mathrm{SE} = 0{,}361$~ms, resultando em estatística $t = 0{,}479$ ($\mathrm{df} = 163$, $p \approx 0{,}63$). O teste \textit{t} pareado, portanto, \textbf{não rejeita} a hipótese nula de que o baseline determinístico não produz redução média de tempo. Adicionalmente, um intervalo de confiança \textit{bootstrap} de 95\% (5\,000 reamostragens) para o \textit{speedup} mediano resultou em [0{,}995;~1{,}007], que inclui $1{,}0$. Em conjunto, esses testes confirmam quantitativamente que, ao nível populacional do HumanEval, a otimização baseada em regras AST tem efeito médio estatisticamente \emph{indistinguível de zero}, sendo os ganhos de fato concentrados em um subconjunto pequeno e identificável de funções recursivas.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\columnwidth]{experiment/results/fig_baseline_hist.pdf}
\caption{Distribuição dos \textit{speedups} (escala $\log_{10}$) para os 155 problemas do HumanEval com tempo mensurável sob o baseline determinístico. A linha pontilhada marca \textit{speedup} = $1\times$.}
\label{fig:hist_baseline}
\end{figure}

A Figura~\ref{fig:hist_baseline} corrobora visualmente a análise: a massa de probabilidade está concentrada em torno de zero (i.e., $1\times$), com cauda longa à esquerda (regressões) e dois outliers extremos à direita (memoização).

\subsection{Detalhamento por Regra Aplicada}

\begin{table}[htbp]
\caption{Detalhe dos problemas nos quais alguma regra do otimizador determinístico foi disparada.}
\label{tab:detalhe}
\centering
\footnotesize
\begin{tabular}{lllrrr}
\hline
\textbf{Task} & \textbf{Função} & \textbf{Regras} & \textbf{Orig.} & \textbf{Otim.} & \textbf{Speedup} \\
 & & & \textbf{(ms)} & \textbf{(ms)} & \\
\hline
HumanEval/63 & fibfib & R1 & 41{,}60 & 0{,}07 & 615{,}4$\times$ \\
HumanEval/55 & fib & R1 & 7{,}97 & 0{,}05 & 151{,}8$\times$ \\
HumanEval/36 & fizz\_buzz & R2 & 1679{,}0 & 1660{,}7 & 1{,}01$\times$ \\
HumanEval/161 & solve & R5 & 0{,}92 & 1{,}02 & 0{,}91$\times$ \\
HumanEval/103 & rounded\_avg & R2 & 5{,}75 & 6{,}81 & 0{,}84$\times$ \\
HumanEval/133 & sum\_sq. & R2 & 0{,}80 & 1{,}35 & 0{,}59$\times$ \\
HumanEval/117 & select\_w. & R4 & 3{,}92 & 7{,}50 & 0{,}52$\times$ \\
HumanEval/74 & total\_match & R2 & 0{,}32 & 0{,}88 & 0{,}36$\times$ \\
\hline
\end{tabular}
\vspace{0.1cm}
\raggedright
\scriptsize R1 = \texttt{lru\_cache}; R2 = \texttt{sum()} builtin; R4 = \texttt{frozenset} lookup; R5 = \texttt{join}
\end{table}

A Tabela~\ref{tab:detalhe} revela um padrão dicotômico. A regra \textbf{R1} (memoização via \texttt{lru\_cache}) produz ganhos excepcionais quando aplicada a funções recursivas (Fibonacci e fibfib), transformando complexidade de $O(2^n)$ em $O(n)$ amortizado. Em contrapartida, as regras \textbf{R2}, \textbf{R4} e \textbf{R5} causam regressões em cinco de seis casos. A razão principal é o \textit{overhead} de construção de objetos intermediários: a expressão geradora criada por R2, o \texttt{frozenset} criado a cada chamada por R4 e a lista temporária criada por R5 superam o custo do laço explícito original para entradas pequenas --- as quais predominam nos testes do HumanEval.

\begin{table}[htbp]
\caption{Frequência de aplicação das regras do otimizador determinístico.}
\label{tab:regras}
\centering
\begin{tabular}{lr}
\hline
\textbf{Regra} & \textbf{Aplicações} \\
\hline
R2 (\texttt{sum()} builtin) & 5 \\
R1 (\texttt{lru\_cache}) & 2 \\
R5 (\texttt{''.join()}) & 1 \\
R4 (\texttt{frozenset} lookup) & 1 \\
\hline
\end{tabular}
\end{table}

\subsection{Análise dos Resultados}

Três observações centrais emergem dos resultados empíricos:

\begin{enumerate}
  \item \textbf{Cobertura limitada das regras simbólicas.} Apenas 8 de 164 problemas (4{,}9\%) ativam alguma regra. Isso demonstra quantitativamente que a correspondência sintática exata (``\texttt{acc = 0; for x in iter: acc += expr}'') é excessivamente rígida: variações triviais de estilo, como o uso de \texttt{enumerate} ou de variáveis auxiliares, escapam à detecção. Essa fragilidade é análoga ao limite dos otimizadores \textit{peephole} em compiladores clássicos.
  \item \textbf{Regressões em transformações localmente corretas.} As regras R2, R4 e R5 produzem código semanticamente correto e sintaticamente idiomático, mas que é \textit{mais lento} na prática. Isso ocorre porque a decisão de otimização foi tomada sem considerar o tamanho típico da entrada --- exatamente o tipo de informação contextual que está \textit{fora} do escopo de regras simbólicas, mas \textit{dentro} do alcance de um LLM com capacidade de raciocínio sobre custo amortizado.
  \item \textbf{Ganhos massivos via transformação algorítmica.} Os dois casos de memoização (R1) comprovam que a maior alavanca de otimização em Python não reside em micro-otimizações de \textit{bytecode}, mas em transformações de nível algorítmico. É precisamente nesse nível que LLMs demonstram maior aptidão, conforme evidenciado pela literatura recente \cite{shypula2023pie,fasterpy2025,sysllmatic2025}.
\end{enumerate}

\subsection{Análise Qualitativa}
Para tornar concretos os fenômenos discutidos, examinamos dois casos representativos extraídos da Tabela~\ref{tab:detalhe}: \textbf{HumanEval/55} (\texttt{fib}), em que a regra R1 produz o segundo maior ganho do experimento ($151{,}8\times$), e \textbf{HumanEval/74} (\texttt{total\_match}), em que a regra R2 produz a pior regressão observada ($0{,}36\times$).

\begin{lstlisting}[caption={Original \texttt{fib} (HumanEval/55).}, label={lst:fib_orig}]
def fib(n: int):
    if n == 0: return 0
    if n == 1: return 1
    return fib(n-1) + fib(n-2)
\end{lstlisting}

\begin{lstlisting}[caption={Versão R1 (\texttt{lru\_cache}) --- $O(n)$ amortizado.}, label={lst:fib_r1}]
from functools import lru_cache

@lru_cache(maxsize=None)
def fib(n: int):
    if n == 0: return 0
    if n == 1: return 1
    return fib(n-1) + fib(n-2)
\end{lstlisting}

A transformação no caso da Listagem~\ref{lst:fib_r1} acrescenta um único decorator e reduz a complexidade observável de $O(2^n)$ para $O(n)$ por chamada amortizada, gerando \textit{speedup} medido de $151{,}8\times$. É o tipo de transformação \emph{algorítmica} que um compilador clássico não realiza, pois exige reconhecer que a função é uma recorrência pura sobre inteiros pequenos.

\begin{lstlisting}[caption={Original \texttt{total\_match} (HumanEval/74).}, label={lst:tm_orig}]
def total_match(lst1, lst2):
    l1 = 0
    for st in lst1:
        l1 += len(st)
    l2 = 0
    for st in lst2:
        l2 += len(st)
    if l1 <= l2: return lst1
    else: return lst2
\end{lstlisting}

\begin{lstlisting}[caption={Versão R2 (\texttt{sum()}) --- regressão $0{,}36\times$.}, label={lst:tm_r2}]
def total_match(lst1, lst2):
    l1 = sum(len(st) for st in lst1)
    l2 = sum(len(st) for st in lst2)
    if l1 <= l2: return lst1
    else: return lst2
\end{lstlisting}

A versão da Listagem~\ref{lst:tm_r2} é \emph{idiomática} em Python e seria considerada superior em uma revisão de código. No entanto, em \textit{benchmarks} com listas pequenas (entradas típicas do HumanEval contêm de 0 a 5 \textit{strings}) o custo de criar e iterar a expressão geradora supera o ganho da implementação \texttt{sum()} em C, resultando em desempenho 2{,}77 vezes pior. Esse é o exemplo paradigmático de transformação em que a \emph{decisão} requer informação de contexto (tamanho típico da entrada, frequência de chamada) que está fora do alcance do casamento sintático --- e dentro do alcance de um modelo de linguagem com capacidade de raciocinar sobre custo amortizado.

\subsection{Comparação Multi-Modelo: Baseline vs. Três LLMs}
\label{sec:llmvsbase}

Para validar empiricamente a hipótese central do artigo --- e separar o efeito do paradigma (LLM em geral) do efeito do modelo específico ---, executamos o mesmo pipeline sobre os 164 problemas do HumanEval substituindo o módulo de transformação por três \textit{back-ends} LLM de provedores e famílias distintas: \textbf{Gemini 2.5 Flash Lite} (Google, API REST), \textbf{Llama 3.3 70B} (Meta open-weight, servido via Groq) e \textbf{GPT-4o-mini} (OpenAI, servido via GitHub Models). Todos foram configurados com \textit{temperature} = $0{,}2$ e \textit{max\_output\_tokens} = 2048, com o mesmo \textit{prompt} estruturado em português, preservando as etapas de validação por testes e \textit{benchmark} (5 repetições $\times$ 200 \textit{workloads}). A Tabela~\ref{tab:llm_vs_base} apresenta a comparação direta entre os quatro back-ends.

\begin{table*}[htbp]
\caption{Comparação pareada sobre os 164 problemas do HumanEval: baseline determinístico vs. três LLMs de famílias distintas.}
\label{tab:llm_vs_base}
\centering
\footnotesize
\begin{tabular}{lrrrr}
\hline
\textbf{Métrica} & \textbf{Baseline AST} & \textbf{Gemini 2.5 FL} & \textbf{Llama 3.3 70B} & \textbf{GPT-4o-mini} \\
\hline
Provedor & --- & Google & Groq (Meta) & GitHub Models (OpenAI) \\
Preservação semântica & \textbf{164/164 (100{,}0\%)} & 154/164 (93{,}9\%) & 126/164 (76{,}8\%) & 135/164 (82{,}3\%) \\
Inválidos (alucinações) & \textbf{0} & 10 & 38 & 29 \\
Melhorias ($>$1{,}05$\times$) & 15 & \textbf{89} & 45 & 61 \\
Neutros [0{,}95;~1{,}05] & 126 & 12 & 22 & 18 \\
Regressões ($<$0{,}95$\times$) & 14 & 44 & 50 & 48 \\
Imensuráveis & 9 & 9 & 9 & 8 \\
\hline
Speedup mediano\textsuperscript{$\dagger$} & 1{,}003$\times$ & \textbf{1{,}138$\times$} & 0{,}976$\times$ & 1{,}018$\times$ \\
IC 95\% (\textit{bootstrap})\textsuperscript{$\dagger$} & [0{,}997;~1{,}008] & \textbf{[1{,}071;~1{,}220]} & [0{,}949;~1{,}015] & [0{,}978;~1{,}133] \\
Significância (sp $\ne 1$) & não & \textbf{sim} & não & não \\
Speedup médio aritm.\textsuperscript{$\dagger$} & 5{,}927$\times$ & \textbf{7{,}109$\times$} & 4{,}044$\times$ & 4{,}266$\times$ \\
Speedup harmônico\textsuperscript{$\dagger$} & 0{,}986$\times$ & \textbf{1{,}064$\times$} & 0{,}859$\times$ & 0{,}956$\times$ \\
Speedup máximo\textsuperscript{$\dagger$} & 615{,}4$\times$ & 299{,}3$\times$ & 122{,}7$\times$ & 133{,}5$\times$ \\
\hline
Cobertura de melhoria & 9{,}1\% & \textbf{54{,}3\%} & 27{,}4\% & 37{,}2\% \\
Razão melhorias/regressões & 1{,}07 & \textbf{2{,}02} & 0{,}90 & 1{,}27 \\
\hline
\end{tabular}
\vspace{0.1cm}
\raggedright
\scriptsize \textsuperscript{$\dagger$}{Estatísticas computadas sobre os problemas com tempo mensurável e código semanticamente válido (sp $>$ 0).}
\end{table*}

A Tabela~\ref{tab:llm_vs_base} revela um padrão mais sofisticado do que a hipótese inicial sugeriria. Cinco observações empíricas se destacam:

\begin{enumerate}
  \item \textbf{Heterogeneidade significativa entre LLMs.} Os três modelos diferem dramaticamente: cobertura de melhoria varia de $27{,}4\%$ (Llama) a $54{,}3\%$ (Gemini), e a taxa de alucinação varia de $6{,}1\%$ (Gemini) a $23{,}2\%$ (Llama). Isso refuta uma leitura ingênua do paradigma ``\textit{LLMs superam baseline}'': o efeito é fortemente dependente do modelo.

  \item \textbf{Significância estatística é exclusiva ao Gemini.} O IC 95\% \textit{bootstrap} para o \textit{speedup} mediano \emph{não inclui} $1{,}0$ apenas no Gemini ($[1{,}071;~1{,}220]$). Llama 70B tem IC $[0{,}949;~1{,}015]$ (inclui $1{,}0$) e GPT-4o-mini $[0{,}978;~1{,}133]$ (inclui $1{,}0$). Ou seja, ao nível populacional do HumanEval, \emph{apenas um dos três LLMs} produz ganho mediano estatisticamente distinguível de zero.

  \item \textbf{Hierarquia de alucinações.} Llama 3.3 70B falha em 38/164 problemas ($23{,}2\%$), quase $4\times$ a taxa do Gemini ($6{,}1\%$); GPT-4o-mini fica em posição intermediária ($17{,}7\%$). Em todos os casos a falha é detectada pelos testes oficiais como assertiva quebrada --- o que confirma que o módulo de validação não é opcional: sem ele, o usuário receberia $17{-}23\%$ de código semanticamente incorreto em produção.

  \item \textbf{Llama 70B é a única configuração em que o LLM regride a mediana.} Apesar de ter mais parâmetros que o Gemini Flash Lite, o Llama tem razão melhorias/regressões $0{,}90 < 1$ e mediana $0{,}976 < 1$. As $50$ regressões superam numericamente as $45$ melhorias; o tamanho do modelo não compensa a calibração relativamente pobre para a tarefa específica de transformação de código.

  \item \textbf{Garantias preservadas em transformações algorítmicas.} Mesmo nos LLMs mais fracos, ganhos extremos foram obtidos em problemas com solução algorítmica clássica: Llama atinge $122{,}7\times$ em $fib$ (memoização), GPT-4o-mini atinge $133{,}5\times$ em $largest\_prime\_factor$ (peneira). Isso indica que \emph{todos} os LLMs conhecem as alavancas algorítmicas; a diferença está em quão frequentemente as aplicam corretamente e em quão bem preservam semântica nos casos restantes.
\end{enumerate}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.98\columnwidth]{experiment/results/fig_comparison_hist.pdf}
\caption{Distribuição de \textit{speedups} (escala $\log_{10}$) para os quatro back-ends. O baseline (cinza) concentra-se em zero (sem efeito). O Gemini (azul) está claramente deslocado para a direita. Llama (laranja) e GPT-4o-mini (verde) ocupam posições intermediárias com modas próximas ou ligeiramente abaixo de zero.}
\label{fig:hist_comparison}
\end{figure}

A Figura~\ref{fig:hist_comparison} torna visualmente saliente a heterogeneidade: enquanto a massa de probabilidade do Gemini está em $\log_{10} \in [0{,}05;~0{,}3]$ (deslocada para a direita), Llama e GPT-4o-mini têm distribuições com modas próximas de zero e caudas pesadas tanto à esquerda (regressões) quanto à direita (ganhos algorítmicos), refletindo o comportamento mais errático observado em suas métricas agregadas.

\subsubsection*{Implicação para a tese central}
A leitura conjunta da Tabela~\ref{tab:llm_vs_base} e da Figura~\ref{fig:hist_comparison} sustenta uma versão mais precisa da hipótese inicial: \emph{LLMs ampliam a fronteira de otimização em relação ao baseline simbólico, mas o ganho efetivo é altamente dependente do modelo escolhido}. Em um pipeline de produção, isso implica que a seleção do LLM deve ser tratada como uma decisão de engenharia validada empiricamente sobre o domínio-alvo --- e não como uma escolha técnica neutra. A presença obrigatória do módulo de validação por testes é o mecanismo que torna esse risco aceitável: ele converte alucinações silenciosas em falhas detectadas, mantendo a preservação semântica efetiva próxima de $100\%$ ao custo de descartar entre $6\%$ e $23\%$ das saídas do LLM.

\section{Discussão: Limites e Possibilidades}
\label{sec:discussao}

\subsection{Limites}
Os resultados experimentais, aliados à revisão da literatura, permitem delinear os seguintes limites:

\begin{itemize}
  \item \textbf{Ausência de garantias formais.} Nem o otimizador determinístico, nem um LLM oferecem provas de equivalência semântica. A corretude depende inteiramente da qualidade da suíte de testes. No experimento, os 164 testes do HumanEval mostraram-se suficientes para o \textit{baseline}, mas cenários reais com cobertura parcial podem não detectar regressões semânticas sutis.
  \item \textbf{Fragilidade simbólica.} Com apenas 4{,}9\% de cobertura, o otimizador determinístico evidencia que transformações por correspondência de padrão são inerentemente limitadas em linguagens dinâmicas, onde variações de estilo, de \textit{framework} e de versão da linguagem multiplicam os padrões possíveis.
  \item \textbf{Sensibilidade ao tamanho da entrada.} Transformações ``benéficas'' podem regredir quando o custo de construção de objetos intermediários domina o tempo de execução para entradas pequenas, como observado em 5 dos 8 casos onde regras foram aplicadas.
  \item \textbf{Alucinações de LLMs.} Modelos de linguagem podem gerar código sintaticamente válido porém semanticamente incorreto. No experimento conduzido (Tabela~\ref{tab:llm_vs_base}), a taxa observada varia entre $6{,}1\%$ (Gemini 2.5 Flash Lite, 10 falhas) e $23{,}2\%$ (Llama 3.3 70B, 38 falhas), com o GPT-4o-mini em posição intermediária ($17{,}7\%$, 29 falhas). Todas as falhas se manifestaram como assertivas quebradas nos testes oficiais. Esse intervalo reforça quantitativamente que (a) o módulo de validação por testes é \emph{obrigatório}, não opcional, e (b) a escolha do LLM tem impacto direto na taxa de descarte resultante.
  \item \textbf{Dinamismo intrínseco do Python.} Mecanismos como \textit{late binding}, \textit{monkey patching}, \texttt{eval}/\texttt{exec} e o GIL permanecem como barreiras fundamentais a otimizações estáticas agressivas, sejam simbólicas ou baseadas em LLM.
\end{itemize}

\subsection{Possibilidades}
Em contrapartida, os resultados e a literatura apontam possibilidades concretas:

\begin{itemize}
  \item \textbf{Transformações algorítmicas de alto nível.} Os \textit{speedups} de 151{,}8$\times$ e 615{,}4$\times$ obtidos com memoização confirmam que o maior potencial de otimização em Python reside em transformações que um compilador clássico não pode inferir. LLMs, treinados em bilhões de \textit{tokens} de código, possuem capacidade de reconhecer padrões algorítmicos e propor reformulações equivalentes \cite{cummins2024metacompiler,fasterpy2025}.
  \item \textbf{Raciocínio contextual.} Diferente de uma regra simbólica, um LLM pode levar em conta o tamanho esperado da entrada, o perfil de uso da função e restrições de domínio presentes na \textit{docstring}, potencialmente evitando as regressões observadas nas regras R2, R4 e R5.
  \item \textbf{Arquitetura híbrida.} A combinação de análise estática via AST (para extrair \textit{features} e filtrar candidatos) com inferência por LLM (para decidir \textit{se} e \textit{como} otimizar) cria um pipeline que recupera parte do rigor de um compilador sem perder a flexibilidade de um modelo probabilístico.
  \item \textbf{Feedback do runtime.} Informações de \textit{profiling} (\texttt{cProfile}), inspeção de \textit{bytecode} (\texttt{dis}) e dados do interpretador adaptativo do CPython 3.11+ podem enriquecer o \textit{prompt} do LLM, conforme já demonstrado pelo CompilerGPT \cite{compilergpt2025}.
  \item \textbf{Reprodutibilidade com custo zero.} A existência de \textit{foundation models} com \textit{tier} gratuito (Gemini 2.5 Flash Lite, Llama 3.3 via Groq) viabiliza a execução do pipeline proposto em contexto acadêmico sem custo, conforme demonstrado neste trabalho.
\end{itemize}

\section{Ameaças à Validade}
\label{sec:ameacas}

Seguindo a taxonomia clássica de pesquisa empírica em engenharia de software, identificam-se as seguintes ameaças aos resultados reportados.

\paragraph{Validade interna} O \textit{benchmark} de tempo de execução é sensível ao ruído do sistema operacional, à variação de \textit{caches} e ao próprio interpretador. Mitigamos esse risco executando 200 repetições do \textit{workload} em cada uma de 5 rodadas e reportando o tempo mínimo, padrão adotado na biblioteca \texttt{timeit}. Ainda assim, \textit{speedups} próximos de $1{,}0\times$ não podem ser interpretados isoladamente e foram, por isso, agrupados na categoria ``neutros''.

\paragraph{Validade de construto} A preservação semântica é aferida pela suíte de testes do HumanEval. Embora a cobertura desses testes seja propositalmente alta para o problema-alvo, equivalência semântica plena requer prova formal, fora do escopo deste trabalho. Transformações localmente corretas mas que dependem de invariantes não testados podem passar despercebidas.

\paragraph{Validade externa} O HumanEval é composto por 164 funções pequenas e autocontidas, com entradas tipicamente reduzidas. Esse perfil favorece micro-benchmarks e penaliza transformações que amortizam custo em entradas grandes (como observado em R2, R4 e R5). A generalização dos achados para código de produção, com classes, dependências externas e \textit{datasets} reais, requer validação adicional sobre repositórios maiores e mais diversos --- lacuna parcialmente endereçada por trabalhos como o DocAgent \cite{yang2025docagent}, que avaliam sistemas baseados em LLM diretamente sobre repositórios reais do GitHub.

\paragraph{Variabilidade do modelo} LLMs são intrinsecamente estocásticos e suas respostas variam com o \textit{prompt}, com o parâmetro de \textit{temperature} e com versões internas do modelo. Os experimentos reportados utilizam \textit{temperature} = $0{,}2$ e o \textit{back-end} determinístico baseado em regras AST como linha de base controlada; resultados específicos do Gemini 2.5 Flash Lite devem ser reproduzidos com seed fixa e versão de modelo congelada antes de qualquer comparação cruzada.

\paragraph{Dataset limitado} O uso exclusivo do HumanEval como universo experimental é uma escolha deliberada por reprodutibilidade, mas restringe a diversidade de domínios. \textit{Datasets} complementares como PIE \cite{shypula2023pie} e Project CodeNet \cite{puri2021codenet} são candidatos naturais para mitigar essa ameaça em trabalhos futuros.

\section{Considerações Finais}
\label{sec:conclusao}

Este artigo investigou os limites e possibilidades do uso de modelos de linguagem na otimização de código Python, com foco no papel de LLMs como componente auxiliar à cadeia de compilação de uma linguagem dinâmica. Foi proposta e implementada uma arquitetura modular (Ingestão $\rightarrow$ Análise Estática via AST $\rightarrow$ Otimização $\rightarrow$ Validação $\rightarrow$ \textit{Benchmark}) operando sobre o dataset público HumanEval (164 problemas).

Os resultados empíricos da execução pareada sobre os 164 problemas do HumanEval, comparando o otimizador determinístico baseado em regras AST e três LLMs de famílias distintas (Gemini 2.5 Flash Lite, Llama 3.3 70B via Groq e GPT-4o-mini via GitHub Models), evidenciaram cinco achados principais:

\begin{enumerate}
\item \textbf{Garantia formal vs. cobertura é um \emph{trade-off} real e mensurável.} O baseline preserva semântica em $100\%$ dos casos mas melhora apenas $9{,}1\%$ dos problemas; os LLMs preservam entre $76{,}8\%$ e $93{,}9\%$ e melhoram entre $27{,}4\%$ e $54{,}3\%$.

\item \textbf{Heterogeneidade entre LLMs é alta e \emph{importa}.} A cobertura de melhoria varia $2\times$ entre os três LLMs ($27{,}4\%$ Llama vs. $54{,}3\%$ Gemini), e a taxa de alucinação varia $4\times$ ($6{,}1\%$ Gemini vs. $23{,}2\%$ Llama). A escolha do modelo não é neutra.

\item \textbf{Apenas um dos três LLMs produz ganho mediano estatisticamente significativo.} O IC $95\%$ \textit{bootstrap} para a mediana do \textit{speedup} não inclui $1{,}0$ somente no Gemini ($[1{,}071;~1{,}220]$); Llama e GPT-4o-mini têm ICs que cruzam $1{,}0$, com o Llama inclusive abaixo ($0{,}976$).

\item \textbf{Ganhos algorítmicos extremos são alcançáveis por todos os LLMs.} Memoização, fórmulas fechadas, \texttt{pow(a,b,m)} e peneira de primos foram redescobertos por todos os modelos com \textit{speedups} entre $100\times$ e $300\times$. A diferença está na frequência de aplicação correta, não na capacidade pontual.

\item \textbf{Validação por testes é mecanismo eficaz de contenção.} Em todos os LLMs, $100\%$ das alucinações foram capturadas pela suíte oficial do HumanEval. Sem o módulo de validação, o usuário receberia entre $6\%$ e $23\%$ de código semanticamente incorreto.
\end{enumerate}

Esses resultados refutam a tese simplista de que ``LLMs superam baseline'' e estabelecem uma tese mais precisa e útil: \emph{abordagens simbólicas são necessárias mas insuficientes; LLMs ampliam a fronteira de otimização ao custo de garantia formal; e a magnitude desse ganho depende fortemente do modelo escolhido}. A integração de LLMs como componente de raciocínio de alto nível em um pipeline com validação semântica obrigatória e seleção de modelo validada empiricamente sobre o domínio-alvo constitui, portanto, uma direção empiricamente validada para contornar os limites impostos pelo dinamismo da linguagem.

Como trabalhos futuros, pretende-se: (i) integrar \textit{Retrieval-Augmented Generation} (RAG) com o dataset PIE \cite{shypula2023pie} como fonte de exemplos de \textit{few-shot}, com expectativa de reduzir a alta taxa de alucinação observada nos LLMs mais fracos (até $23{,}2\%$ no Llama 70B); (ii) incorporar \textit{feedback} de \textit{bytecode} do CPython 3.11+ e relatórios de \textit{profiling} como contexto adicional ao \textit{prompt} do LLM, conforme já demonstrado pelo CompilerGPT \cite{compilergpt2025}; (iii) avaliar a generalização para outros \textit{datasets} de complexidade maior (Project CodeNet Python800, MBPP, repositórios reais do GitHub) e para LLMs especializados em código (DeepSeek-Coder, StarCoder2, Code Llama) sob o mesmo protocolo de comparação pareada; (iv) implementar um \textit{loop} de \textit{self-refine} em que falhas de validação retornam ao LLM como contexto para uma nova tentativa, visando recuperar parte das alucinações; (v) investigar \textbf{por que} a heterogeneidade entre LLMs é tão grande --- particularmente, se a diferença entre Gemini ($6{,}1\%$ de alucinação) e Llama 70B ($23{,}2\%$) decorre de tamanho de modelo, dados de treinamento, técnicas de \textit{alignment} ou tunagem de \textit{prompt}; e (vi) explorar uma arquitetura multi-agente inspirada no DocAgent \cite{yang2025docagent}, na qual agentes especializados em análise, transformação e verificação cooperam por meio de um \textit{orchestrator}, com travessia dependência-consciente sobre o grafo de chamadas do programa.

\paragraph{Contribuições esperadas} Em síntese, o presente trabalho contribui com: (1) uma avaliação crítica, reprodutível e \emph{multi-modelo} do uso de IA generativa para otimização de código em uma linguagem dinâmica, comparando três LLMs de provedores e famílias distintas; (2) a identificação quantitativa dos limites reais de abordagens simbólicas baseadas em padrão sobre AST (cobertura de apenas $4{,}9\%$ no HumanEval, sem ganho mediano estatisticamente significativo); (3) a evidência empírica de \emph{forte heterogeneidade entre LLMs} para a tarefa de otimização de código (alucinação variando $4\times$, cobertura variando $2\times$, apenas um dos três modelos atingindo significância estatística no \textit{speedup} mediano); e (4) diretrizes para o uso seguro de LLMs em pipelines de otimização --- notadamente, a obrigatoriedade de validação semântica por testes, a necessidade de seleção empírica do modelo sobre o domínio-alvo (não bastando assumir que um LLM ``moderno'' funciona) e a adoção de uma malha de verificação inspirada em sistemas como o DocAgent \cite{yang2025docagent}.

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
